#include"../hpp/headers.hpp"

int main(int ac, char **av, char **env)
{
	// int a =  fork();
	// if (a == 0)
	// {
	// 	if (freopen("output.txt", "w", stdout));
	// 		execve("cgi", av, env);
	// 	exit(0);
	// }
	// else
	// {

	// }

	double a = 31;
	std::cout << a << "\n";
	return 0;
}